import { GOLDEN_FIXTURES } from './harness.js';
import { EXTENDED_FIXTURES } from './fixtures-extended.js';

/** Top 25 card fixtures for benefit seeding (5 golden + 20 extended). */
export const TOP25_FIXTURES = [...GOLDEN_FIXTURES, ...EXTENDED_FIXTURES];
