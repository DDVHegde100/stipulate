#!/usr/bin/env sh
if grep -q '^Co-authored-by: Cursor <cursoragent@cursor.com>$' "$1"; then
  echo "error: Co-authored-by Cursor trailers are not permitted in this repository" >&2
  exit 1
fi
pnpm exec commitlint --edit "$1"
